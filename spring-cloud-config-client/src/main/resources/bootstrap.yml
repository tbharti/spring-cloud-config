spring:
  application:
    name: spring-cloud-config-client
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: admin
      fail-fast: true